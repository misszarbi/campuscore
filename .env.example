# Copier ce fichier en .env et remplacer les valeurs avant de lancer docker compose
# Ne jamais remplir de vraies valeurs ici, dans .env.example

MONGO_ROOT_PASSWORD=changeme
GRAFANA_ADMIN_PASSWORD=changeme
PORTAL_JWT_SECRET=changeme
